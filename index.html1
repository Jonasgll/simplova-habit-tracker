<!DOCTYPE html>

<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
<title>Habit Tracker – SimplovaStudio</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.0/chart.umd.min.js"></script>
<style>
:root {
  --accent:#06D6A0; --accent-rgb:6,214,160;
  --bg:#fff; --bg2:#f7f8fa; --bg3:#eef0f3;
  --card:#fff; --border:#e8eaed;
  --text:#1a1a2e; --text2:#6b7280; --text3:#9ca3af;
  --nav-h:68px; --radius:16px; --radius-sm:10px;
  --font:'DM Sans',sans-serif; --mono:'DM Mono',monospace;
}
[data-theme="dark"] {
  --bg:#0f0f1a; --bg2:#1a1a2e; --bg3:#252538;
  --card:#1e1e32; --border:#2e2e48;
  --text:#f0f0ff; --text2:#8888aa; --text3:#55557a;
}
*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{height:100%}
body{font-family:var(--font);background:var(--bg);color:var(--text);transition:background .25s,color .25s;overflow:hidden}
.screen{display:none;height:100vh;flex-direction:column}
.screen.active{display:flex}

/* ONBOARDING */
#ob-screen{background:var(–bg);align-items:center;justify-content:center;padding:2rem}
.ob-inner{width:100%;max-width:400px}
.ob-brand{font-size:12px;font-weight:500;letter-spacing:.1em;text-transform:uppercase;color:var(–text3);margin-bottom:2.5rem}
.ob-brand b{color:var(–accent);font-weight:600}
.ob-dots{display:flex;gap:6px;margin-bottom:2rem}
.ob-dot{width:6px;height:6px;border-radius:50%;background:var(–bg3);transition:.3s}
.ob-dot.active{width:20px;border-radius:3px;background:var(–accent)}
.ob-title{font-size:26px;font-weight:600;line-height:1.2;margin-bottom:.5rem}
.ob-sub{font-size:15px;color:var(–text2);margin-bottom:2rem;line-height:1.6}
.ob-input{width:100%;padding:14px 16px;border:1.5px solid var(–border);border-radius:var(–radius-sm);font-family:var(–font);font-size:16px;background:var(–bg2);color:var(–text);outline:none;transition:.2s;margin-bottom:1rem}
.ob-input:focus{border-color:var(–accent)}
.ob-lbl{font-size:12px;font-weight:500;color:var(–text2);text-transform:uppercase;letter-spacing:.06em;margin-bottom:.6rem;display:block}
.ob-colors{display:flex;gap:10px;margin-bottom:1.5rem;flex-wrap:wrap}
.ob-color{width:42px;height:42px;border-radius:50%;cursor:pointer;border:3px solid transparent;transition:.2s;display:flex;align-items:center;justify-content:center;color:transparent;font-size:18px;font-weight:700}
.ob-color.sel{border-color:var(–text);color:#fff;transform:scale(1.1)}
.ob-freqs{display:flex;gap:8px;margin-bottom:1.5rem;flex-wrap:wrap}
.ob-freq{padding:8px 16px;border-radius:20px;border:1.5px solid var(–border);background:var(–bg2);color:var(–text2);font-family:var(–font);font-size:13px;cursor:pointer;transition:.2s}
.ob-freq.sel{background:var(–accent);border-color:var(–accent);color:#fff;font-weight:500}
.ob-btn{width:100%;padding:16px;background:var(–accent);color:#fff;border:none;border-radius:var(–radius-sm);font-family:var(–font);font-size:15px;font-weight:600;cursor:pointer;transition:.15s}
.ob-btn:hover{opacity:.9}
.ob-btn:active{transform:scale(.98)}

/* APP */
#app-screen{background:var(–bg2)}
.hdr{display:flex;align-items:center;justify-content:space-between;padding:.9rem 1.25rem;background:var(–bg);border-bottom:1px solid var(–border);flex-shrink:0;z-index:50}
.hdr-left{}
.hdr-greet{font-size:12px;color:var(–text3)}
.hdr-title{font-size:17px;font-weight:600}
.hdr-right{display:flex;gap:6px;align-items:center}
.hbtn{height:34px;border-radius:8px;border:1px solid var(–border);background:var(–bg2);color:var(–text2);font-family:var(–font);cursor:pointer;transition:.15s;display:flex;align-items:center;justify-content:center}
.hbtn:hover{background:var(–bg3)}
.hbtn.icon{width:34px;font-size:15px}
.hbtn.lang{padding:0 10px;font-family:var(–mono);font-size:11px;font-weight:500}

/* PAGES */
.page{display:none;flex:1;overflow-y:auto;padding:1.25rem;padding-bottom:calc(var(–nav-h) + 1rem)}
.page.active{display:block}

/* ── RESPONSIVE ── */

/* TABLET (768px+): centered container, 2-col layout */
@media(min-width:768px){
body{overflow:auto}
.screen{height:100vh}
#app-screen{display:grid;grid-template-rows:auto 1fr auto;background:var(–bg2)}
.hdr{padding:.9rem 2rem;max-width:100%}
.page{padding:1.5rem 2rem;padding-bottom:calc(var(–nav-h) + 1rem);max-width:900px;margin:0 auto;width:100%}
.kpi-grid{grid-template-columns:repeat(4,1fr)}
.week-days{gap:8px}
.wday-dot{max-width:48px;font-size:13px}
.habits-list{gap:10px}
.habit-row{padding:1rem 1.25rem}
.hcheck{width:36px;height:36px;font-size:18px}
.hname{font-size:15px}
.bottom-nav{max-width:100%}
.nav-item{max-width:120px;margin:0 auto}
.ob-inner{max-width:480px}
}

/* DESKTOP (1024px+): sidebar nav + main content */
@media(min-width:1024px){
body{overflow:hidden}
#app-screen{display:grid;grid-template-columns:220px 1fr;grid-template-rows:auto 1fr;grid-template-areas:‘hdr hdr’ ‘nav main’;height:100vh}
.hdr{grid-area:hdr;padding:.9rem 2rem;border-bottom:1px solid var(–border)}
.bottom-nav{grid-area:nav;height:100%;flex-direction:column;justify-content:flex-start;padding-top:1.5rem;gap:4px;border-top:none;border-right:1px solid var(–border);align-items:stretch;background:var(–bg)}
.nav-item{flex-direction:row;justify-content:flex-start;gap:12px;padding:.75rem 1.25rem;border-radius:10px;margin:0 .75rem;max-width:none}
.nav-icon{font-size:18px}
.nav-lbl{font-size:13px;text-transform:none;letter-spacing:0;font-family:var(–font);font-weight:500}
.page{grid-area:main;padding:2rem;padding-bottom:2rem;overflow-y:auto;max-width:800px;margin:0 auto;width:100%}
.kpi-grid{grid-template-columns:repeat(4,1fr)}
.kpi-val{font-size:26px}
.habits-list{gap:10px}
.habit-row{padding:1rem 1.25rem}
.hcheck{width:36px;height:36px;font-size:18px}
.hname{font-size:15px;white-space:normal}
.hm-cell{width:16px;height:16px}
.chart-wrap{height:220px}
.badges-grid{grid-template-columns:repeat(6,1fr)}
.modal-ov{align-items:center;justify-content:center}
.modal{border-radius:var(–radius);max-width:480px;width:100%;animation:fadeIn .2s ease}
@keyframes fadeIn{from{opacity:0;transform:scale(.97)}to{opacity:1;transform:scale(1)}}
}

/* KPI */
.kpi-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-bottom:1.25rem}
.kpi{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:.9rem 1rem}
.kpi-lbl{font-size:11px;color:var(–text3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:.3rem}
.kpi-val{font-family:var(–mono);font-size:22px;font-weight:500;line-height:1}
.kpi-val.ac{color:var(–accent)}
.kpi-sub{font-size:11px;color:var(–text3);margin-top:.2rem}

/* SECTION HDR */
.sh{display:flex;align-items:center;justify-content:space-between;margin-bottom:.75rem}
.sh-t{font-size:14px;font-weight:600}
.sh-a{font-size:13px;color:var(–accent);cursor:pointer;font-weight:500}

/* QUOTE */
.quote{background:var(–accent);border-radius:var(–radius);padding:1rem 1.25rem;margin-bottom:1.25rem}
.quote-text{font-size:14px;color:#fff;line-height:1.5;font-style:italic}
.quote-author{font-size:11px;color:rgba(255,255,255,.7);margin-top:.4rem;font-family:var(–mono)}

/* HABITS */
.habits-list{display:flex;flex-direction:column;gap:8px;margin-bottom:1.25rem}
.habit-row{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:.9rem 1rem;display:flex;align-items:center;gap:.9rem;transition:.2s}
.habit-row.done{border-color:rgba(var(–accent-rgb),.35);background:rgba(var(–accent-rgb),.05)}
.hcheck{width:30px;height:30px;border-radius:50%;border:2px solid var(–border);cursor:pointer;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:16px;transition:.2s}
.hcheck.checked{color:#fff;border-color:transparent}
.hinfo{flex:1;min-width:0}
.hname{font-size:14px;font-weight:500;margin-bottom:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.hmeta{display:flex;gap:10px;align-items:center}
.hstreak{font-family:var(–mono);font-size:11px;color:var(–text3)}
.hfreq{font-size:11px;color:var(–text3)}
.hprog-wrap{height:3px;background:var(–bg3);border-radius:2px;margin-top:5px;overflow:hidden}
.hprog{height:100%;border-radius:2px;transition:.4s}
.hdot{width:10px;height:10px;border-radius:50%;flex-shrink:0}

/* WEEK */
.week-card{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:1rem;margin-bottom:1.25rem}
.week-days{display:flex;justify-content:space-between;gap:4px;margin-top:.75rem}
.wday{flex:1;display:flex;flex-direction:column;align-items:center;gap:5px}
.wday-lbl{font-size:10px;color:var(–text3);font-family:var(–mono);text-transform:uppercase}
.wday-dot{width:100%;max-width:36px;aspect-ratio:1;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:500;transition:.2s}
.wday-dot.today{box-shadow:0 0 0 2px var(–accent)}
.wday-dot.full{background:var(–accent);color:#fff}
.wday-dot.part{background:rgba(var(–accent-rgb),.3);color:var(–text)}
.wday-dot.none{background:var(–bg3);color:var(–text3)}
.wday-dot.future{background:var(–bg2);color:var(–text3)}

/* HEATMAP */
.hm-card{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:1rem;margin-bottom:1.25rem;overflow:hidden}
.hm-month{font-size:12px;font-weight:500;color:var(–text2);margin-bottom:.75rem}
.hm-grid{display:flex;gap:3px;overflow-x:auto;padding-bottom:4px}
.hm-col{display:flex;flex-direction:column;gap:3px}
.hm-cell{width:13px;height:13px;border-radius:3px;flex-shrink:0}
.hm-cell.l0{background:var(–bg3)}
.hm-cell.l1{background:rgba(var(–accent-rgb),.25)}
.hm-cell.l2{background:rgba(var(–accent-rgb),.5)}
.hm-cell.l3{background:rgba(var(–accent-rgb),.75)}
.hm-cell.l4{background:var(–accent)}
.hm-legend{display:flex;align-items:center;gap:4px;margin-top:.6rem}
.hm-leg-lbl{font-size:10px;color:var(–text3)}
.hm-leg-cells{display:flex;gap:3px}

/* CHARTS */
.chart-card{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:1rem;margin-bottom:1rem}
.chart-title{font-size:13px;font-weight:500;color:var(–text2);margin-bottom:.75rem}
.chart-wrap{position:relative;height:180px}

/* BADGES */
.badges-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:1.25rem}
.badge-item{background:var(–card);border:1px solid var(–border);border-radius:var(–radius-sm);padding:.75rem .5rem;text-align:center}
.badge-icon{font-size:24px;margin-bottom:.3rem;filter:grayscale(1);opacity:.35;transition:.3s}
.badge-icon.earned{filter:none;opacity:1}
.badge-lbl{font-size:10px;color:var(–text3);font-family:var(–mono)}

/* MANAGE */
.add-btn{width:100%;padding:14px;border:1.5px dashed var(–border);border-radius:var(–radius);background:transparent;color:var(–text2);font-family:var(–font);font-size:14px;cursor:pointer;transition:.2s;margin-bottom:1rem;display:flex;align-items:center;justify-content:center;gap:8px}
.add-btn:hover{border-color:var(–accent);color:var(–accent);background:rgba(var(–accent-rgb),.04)}
.mh-row{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);padding:.9rem 1rem;display:flex;align-items:center;gap:.75rem;margin-bottom:8px}
.mh-dot{width:12px;height:12px;border-radius:50%;flex-shrink:0}
.mh-info{flex:1;min-width:0}
.mh-name{font-size:14px;font-weight:500}
.mh-sub{font-size:11px;color:var(–text3);margin-top:2px}
.mh-actions{display:flex;gap:6px}
.mh-btn{width:30px;height:30px;border-radius:8px;border:1px solid var(–border);background:var(–bg2);cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:13px;transition:.15s;color:var(–text2)}
.mh-btn:hover{background:var(–bg3)}
.mh-btn.del:hover{background:#fee2e2;border-color:#fca5a5;color:#dc2626}

/* SETTINGS */
.set-section{background:var(–card);border:1px solid var(–border);border-radius:var(–radius);overflow:hidden;margin-bottom:1rem}
.set-row{display:flex;align-items:center;justify-content:space-between;padding:1rem 1.25rem;border-bottom:1px solid var(–border)}
.set-row:last-child{border-bottom:none}
.set-lbl{font-size:14px;font-weight:500}
.set-sub{font-size:12px;color:var(–text3);margin-top:2px}
.toggle{width:44px;height:24px;border-radius:12px;background:var(–bg3);cursor:pointer;position:relative;transition:.2s;border:none;flex-shrink:0}
.toggle.on{background:var(–accent)}
.toggle::after{content:’’;position:absolute;width:18px;height:18px;border-radius:50%;background:#fff;top:3px;left:3px;transition:.2s}
.toggle.on::after{left:23px}
.accent-picker{display:flex;gap:8px}
.ac-opt{width:28px;height:28px;border-radius:50%;cursor:pointer;border:2px solid transparent;transition:.2s;display:flex;align-items:center;justify-content:center;color:transparent;font-size:12px}
.ac-opt.sel{border-color:var(–text);color:#fff}
.danger{color:#ef476f;cursor:pointer;font-size:14px;font-weight:500}

/* NAV */
.bottom-nav{height:var(–nav-h);background:var(–bg);border-top:1px solid var(–border);display:flex;align-items:center;justify-content:space-around;flex-shrink:0;padding-bottom:env(safe-area-inset-bottom)}
.nav-item{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;cursor:pointer;padding:.5rem;transition:.15s;color:var(–text3);border:none;background:none}
.nav-item.active{color:var(–accent)}
.nav-icon{font-size:20px;line-height:1}
.nav-lbl{font-size:10px;font-family:var(–mono);font-weight:500;text-transform:uppercase;letter-spacing:.04em}

/* MODAL */
.modal-ov{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:200;align-items:flex-end}
.modal-ov.open{display:flex}
.modal{background:var(–card);border-radius:var(–radius) var(–radius) 0 0;padding:1.5rem;width:100%;max-height:85vh;overflow-y:auto;animation:slideUp .25s ease}
@keyframes slideUp{from{transform:translateY(40px);opacity:0}to{transform:translateY(0);opacity:1}}
.modal-title{font-size:18px;font-weight:600;margin-bottom:1.25rem}
.modal-input{width:100%;padding:13px 16px;border:1.5px solid var(–border);border-radius:var(–radius-sm);font-family:var(–font);font-size:15px;background:var(–bg2);color:var(–text);outline:none;transition:.2s;margin-bottom:1rem}
.modal-input:focus{border-color:var(–accent)}
.modal-actions{display:flex;gap:8px;margin-top:.5rem}
.modal-btn{flex:1;padding:13px;border-radius:var(–radius-sm);border:none;font-family:var(–font);font-size:14px;font-weight:600;cursor:pointer;transition:.15s}
.modal-btn.primary{background:var(–accent);color:#fff}
.modal-btn.secondary{background:var(–bg3);color:var(–text)}

/* CONFETTI */
.conf-wrap{position:fixed;inset:0;pointer-events:none;z-index:999;overflow:hidden}
.conf-piece{position:absolute;animation:confFall linear forwards}
@keyframes confFall{0%{transform:translateY(-20px) rotate(0deg);opacity:1}100%{transform:translateY(100vh) rotate(720deg);opacity:0}}

/* EMPTY */
.empty{text-align:center;padding:3rem 1rem;color:var(–text3)}
.empty-icon{font-size:40px;margin-bottom:.75rem;opacity:.5}
.empty-text{font-size:14px}
</style>

</head>
<body>

<!-- ONBOARDING -->

<div id="ob-screen" class="screen active">
  <div class="ob-inner">
    <div class="ob-brand">Simplova<b>Studio</b></div>
    <div class="ob-dots"><div class="ob-dot active" id="dot1"></div><div class="ob-dot" id="dot2"></div></div>
    <div id="ob1">
      <div class="ob-title">Willkommen! 👋</div>
      <div class="ob-sub" id="ob1-sub">Wie sollen wir dich nennen?</div>
      <input class="ob-input" id="ob-name" placeholder="Dein Name" maxlength="30">
      <button class="ob-btn" onclick="obNext()">Weiter →</button>
    </div>
    <div id="ob2" style="display:none">
      <div class="ob-title" id="ob2-title">Deine erste Gewohnheit</div>
      <div class="ob-sub" id="ob2-sub">Womit möchtest du heute starten?</div>
      <input class="ob-input" id="ob-habit" placeholder="z.B. Täglich 30 Min lesen" maxlength="40">
      <label class="ob-lbl" id="ob-cl">Farbe wählen</label>
      <div class="ob-colors" id="ob-colors"></div>
      <label class="ob-lbl" id="ob-fl">Häufigkeit</label>
      <div class="ob-freqs" id="ob-freqs"></div>
      <button class="ob-btn" onclick="obFinish()">Loslegen →</button>
    </div>
  </div>
</div>

<!-- APP -->

<div id="app-screen" class="screen">
  <div class="hdr">
    <div class="hdr-left">
      <div class="hdr-greet" id="h-greet"></div>
      <div class="hdr-title" data-de="Habit Tracker" data-en="Habit Tracker">Habit Tracker</div>
    </div>
    <div class="hdr-right">
      <button class="hbtn lang" onclick="toggleLang()" id="lang-btn">DE</button>
      <button class="hbtn icon" onclick="toggleTheme()" id="theme-btn">🌙</button>
    </div>
  </div>

  <!-- PAGE: TODAY -->

  <div id="page-today" class="page active">
    <div class="quote" id="quote-card">
      <div class="quote-text" id="quote-text"></div>
      <div class="quote-author" id="quote-author"></div>
    </div>
    <div class="kpi-grid">
      <div class="kpi"><div class="kpi-lbl" data-de="Heute erledigt" data-en="Done today">Heute erledigt</div><div class="kpi-val ac" id="kpi-done">0</div><div class="kpi-sub" id="kpi-done-sub">von 0 Habits</div></div>
      <div class="kpi"><div class="kpi-lbl" data-de="Beste Streak" data-en="Best streak">Beste Streak</div><div class="kpi-val" id="kpi-best">0</div><div class="kpi-sub" data-de="Tage" data-en="days">Tage</div></div>
      <div class="kpi"><div class="kpi-lbl" data-de="Erfolgsquote" data-en="Success rate">Erfolgsquote</div><div class="kpi-val ac" id="kpi-rate">0%</div><div class="kpi-sub" data-de="diese Woche" data-en="this week">diese Woche</div></div>
      <div class="kpi"><div class="kpi-lbl" data-de="Aktive Habits" data-en="Active habits">Aktive Habits</div><div class="kpi-val" id="kpi-active">0</div><div class="kpi-sub" data-de="von 15 max." data-en="of 15 max">von 15 max.</div></div>
    </div>
    <div class="sh"><span class="sh-t" data-de="Wochenübersicht" data-en="Week overview">Wochenübersicht</span></div>
    <div class="week-card"><div class="week-days" id="week-days"></div></div>
    <div class="sh">
      <span class="sh-t" data-de="Heutige Habits" data-en="Today's Habits">Heutige Habits</span>
      <span class="sh-a" onclick="navTo('manage')" data-de="+ Hinzufügen" data-en="+ Add">+ Hinzufügen</span>
    </div>
    <div class="habits-list" id="today-habits"></div>
  </div>

  <!-- PAGE: STATS -->

  <div id="page-stats" class="page">
    <div class="sh"><span class="sh-t" data-de="Aktivitäts-Heatmap" data-en="Activity Heatmap">Aktivitäts-Heatmap</span></div>
    <div class="hm-card">
      <div class="hm-month" id="hm-month"></div>
      <div class="hm-grid" id="hm-grid"></div>
      <div class="hm-legend">
        <span class="hm-leg-lbl" data-de="Weniger" data-en="Less">Weniger</span>
        <div class="hm-leg-cells"><div class="hm-cell l0"></div><div class="hm-cell l1"></div><div class="hm-cell l2"></div><div class="hm-cell l3"></div><div class="hm-cell l4"></div></div>
        <span class="hm-leg-lbl" data-de="Mehr" data-en="More">Mehr</span>
      </div>
    </div>
    <div class="sh"><span class="sh-t" data-de="Wochenvergleich" data-en="Weekly progress">Wochenvergleich</span></div>
    <div class="chart-card"><div class="chart-wrap"><canvas id="chart-week"></canvas></div></div>
    <div class="sh"><span class="sh-t" data-de="Meilenstein-Badges" data-en="Milestone Badges">Meilenstein-Badges</span></div>
    <div class="badges-grid" id="badges-grid"></div>
  </div>

  <!-- PAGE: MANAGE -->

  <div id="page-manage" class="page">
    <button class="add-btn" onclick="openAddModal()" id="add-btn">
      <span style="font-size:18px">+</span>
      <span data-de="Neue Gewohnheit hinzufügen" data-en="Add new habit">Neue Gewohnheit hinzufügen</span>
    </button>
    <div id="manage-list"></div>
  </div>

  <!-- PAGE: SETTINGS -->

  <div id="page-settings" class="page">
    <div class="sh"><span class="sh-t" data-de="Darstellung" data-en="Appearance">Darstellung</span></div>
    <div class="set-section">
      <div class="set-row">
        <div><div class="set-lbl" data-de="Dark Mode" data-en="Dark Mode">Dark Mode</div></div>
        <button class="toggle" id="dark-toggle" onclick="toggleTheme()"></button>
      </div>
      <div class="set-row">
        <div><div class="set-lbl" data-de="Akzentfarbe" data-en="Accent color">Akzentfarbe</div><div class="set-sub" data-de="Deine persönliche Farbe" data-en="Your personal color">Deine persönliche Farbe</div></div>
        <div class="accent-picker" id="accent-picker"></div>
      </div>
    </div>
    <div class="sh"><span class="sh-t" data-de="Sprache" data-en="Language">Sprache</span></div>
    <div class="set-section">
      <div class="set-row">
        <div><div class="set-lbl">Deutsch / English</div></div>
        <button class="toggle" id="lang-toggle" onclick="toggleLang()"></button>
      </div>
    </div>
    <div class="sh"><span class="sh-t" data-de="Daten" data-en="Data">Daten</span></div>
    <div class="set-section">
      <div class="set-row" onclick="resetAll()" style="cursor:pointer">
        <div><div class="danger" data-de="Alle Daten zurücksetzen" data-en="Reset all data">Alle Daten zurücksetzen</div><div class="set-sub" data-de="Nicht rückgängig machbar" data-en="Cannot be undone">Nicht rückgängig machbar</div></div>
        <span style="color:var(--text3)">›</span>
      </div>
    </div>
    <div style="text-align:center;margin-top:2rem">
      <div style="font-size:11px;color:var(--text3);font-family:var(--mono)">SimplovaStudio Habit Tracker v1.0</div>
      <div style="font-size:11px;color:var(--text3);margin-top:4px">Cleane Vorlagen. Einfaches Leben.</div>
    </div>
  </div>

  <nav class="bottom-nav">
    <button class="nav-item active" onclick="navTo('today')" id="nav-today"><span class="nav-icon">✓</span><span class="nav-lbl" data-de="Heute" data-en="Today">Heute</span></button>
    <button class="nav-item" onclick="navTo('stats')" id="nav-stats"><span class="nav-icon">📊</span><span class="nav-lbl" data-de="Statistik" data-en="Stats">Statistik</span></button>
    <button class="nav-item" onclick="navTo('manage')" id="nav-manage"><span class="nav-icon">📋</span><span class="nav-lbl" data-de="Habits" data-en="Habits">Habits</span></button>
    <button class="nav-item" onclick="navTo('settings')" id="nav-settings"><span class="nav-icon">⚙️</span><span class="nav-lbl" data-de="Einstellungen" data-en="Settings">Einstellungen</span></button>
  </nav>
</div>

<!-- MODAL -->

<div class="modal-ov" id="habit-modal" onclick="closeModalOv(event)">
  <div class="modal">
    <div class="modal-title" id="modal-title">Neue Gewohnheit</div>
    <input class="modal-input" id="modal-name" placeholder="Name der Gewohnheit" maxlength="40">
    <label class="ob-lbl" id="m-cl" style="margin-bottom:.6rem">Farbe</label>
    <div class="ob-colors" id="modal-colors" style="margin-bottom:1.25rem"></div>
    <label class="ob-lbl" id="m-fl" style="margin-bottom:.6rem">Häufigkeit</label>
    <div class="ob-freqs" id="modal-freqs" style="margin-bottom:1.25rem"></div>
    <div class="modal-actions">
      <button class="modal-btn secondary" onclick="document.getElementById('habit-modal').classList.remove('open')" id="m-cancel">Abbrechen</button>
      <button class="modal-btn primary" onclick="saveHabit()" id="m-save">Speichern</button>
    </div>
  </div>
</div>

<!-- RESET MODAL -->

<div class="modal-ov" id="reset-modal">
  <div class="modal" style="text-align:center">
    <div style="font-size:32px;margin-bottom:.75rem">⚠️</div>
    <div class="modal-title" style="text-align:center" id="rm-title">Alle Daten löschen?</div>
    <div style="font-size:14px;color:var(--text2);margin-bottom:1.5rem;line-height:1.6" id="rm-sub">Alle Habits und Einträge werden unwiderruflich gelöscht.</div>
    <div class="modal-actions">
      <button class="modal-btn secondary" onclick="cancelReset()" id="rm-cancel">Abbrechen</button>
      <button class="modal-btn primary" onclick="confirmReset()" style="background:#ef476f" id="rm-confirm">Löschen</button>
    </div>
  </div>
</div>

<div class="conf-wrap" id="conf-wrap"></div>

<script>
const ACCENTS=[{c:'#06D6A0',n:'Emerald'},{c:'#7B61FF',n:'Violet'},{c:'#EF476F',n:'Coral'},{c:'#FFD166',n:'Amber'}];
const FREQS_DE=['Täglich','Mo–Fr','3x/Woche','2x/Woche','Wöchentlich'];
const FREQS_EN=['Daily','Mon–Fri','3x/week','2x/week','Weekly'];
const QUOTES_DE=[
  {t:'Kleine Schritte führen zu großen Veränderungen.',a:'— Lao-Tzu'},
  {t:'Disziplin ist die Brücke zwischen Zielen und Erfolg.',a:'— Jim Rohn'},
  {t:'Du wirst, wer du täglich tust.',a:'— Aristoteles'},
  {t:'Gewohnheiten formen Charakter. Charakter formt Schicksal.',a:'— Heraklit'},
  {t:'Motivation bringt dich in Gang. Gewohnheit hält dich am Laufen.',a:'— Jim Ryun'},
  {t:'Der beste Zeitpunkt war gestern. Der zweitbeste ist jetzt.',a:'— Chinesisches Sprichwort'},
  {t:'Erfolg ist die Summe kleiner Anstrengungen, Tag für Tag.',a:'— Robert Collier'},
];
const QUOTES_EN=[
  {t:'Small steps lead to big changes.',a:'— Lao Tzu'},
  {t:'Discipline is the bridge between goals and accomplishment.',a:'— Jim Rohn'},
  {t:'You are what you repeatedly do.',a:'— Aristotle'},
  {t:'Habits shape character. Character shapes destiny.',a:'— Heraclitus'},
  {t:'Motivation gets you going, habit keeps you going.',a:'— Jim Ryun'},
  {t:'The best time to start was yesterday. The second best is now.',a:'— Chinese Proverb'},
  {t:'Success is the sum of small efforts, repeated day in and day out.',a:'— Robert Collier'},
];
const BADGES=[
  {icon:'🔥',days:7,de:'7 Tage',en:'7 Days'},
  {icon:'⚡',days:14,de:'14 Tage',en:'14 Days'},
  {icon:'🌟',days:21,de:'21 Tage',en:'21 Days'},
  {icon:'🏆',days:30,de:'30 Tage',en:'30 Days'},
  {icon:'💎',days:60,de:'60 Tage',en:'60 Days'},
  {icon:'👑',days:100,de:'100 Tage',en:'100 Days'},
];

let S={name:'',lang:'de',dark:false,accent:'#06D6A0',habits:[],log:{}};
let obColor=ACCENTS[0].c, obFreq=0, mColor=ACCENTS[0].c, mFreq=0, editId=null, wChart=null;

function save(){localStorage.setItem('ht2_s',JSON.stringify(S))}
function load(){const d=localStorage.getItem('ht2_s');if(d)S={...S,...JSON.parse(d)}}

function today(){return new Date().toISOString().split('T')[0]}
function dstr(d){return d.toISOString().split('T')[0]}
function addD(date,n){const d=new Date(date);d.setDate(d.getDate()+n);return d}
function weekStart(){const d=new Date();d.setDate(d.getDate()-((d.getDay()||7)-1));return d}

// ONBOARDING
function initOb(){
  document.getElementById('ob-colors').innerHTML=ACCENTS.map((a,i)=>`<div class="ob-color${i===0?' sel':''}" style="background:${a.c}" data-c="${a.c}" onclick="pickObColor(this,'ob-colors')">✓</div>`).join('');
  document.getElementById('ob-freqs').innerHTML=FREQS_DE.map((f,i)=>`<div class="ob-freq${i===0?' sel':''}" data-i="${i}" onclick="pickObFreq(this,'ob-freqs')">${f}</div>`).join('');
}
function pickObColor(el,listId){document.querySelectorAll(`#${listId} .ob-color`).forEach(e=>e.classList.remove('sel'));el.classList.add('sel');obColor=el.dataset.c}
function pickObFreq(el,listId){document.querySelectorAll(`#${listId} .ob-freq`).forEach(e=>e.classList.remove('sel'));el.classList.add('sel');obFreq=parseInt(el.dataset.i)}
function obNext(){
  const n=document.getElementById('ob-name').value.trim();
  if(!n){document.getElementById('ob-name').focus();return}
  S.name=n;
  document.getElementById('ob1').style.display='none';
  document.getElementById('ob2').style.display='block';
  document.getElementById('dot1').classList.remove('active');
  document.getElementById('dot2').classList.add('active');
}
function obFinish(){
  const n=document.getElementById('ob-habit').value.trim();
  if(!n){document.getElementById('ob-habit').focus();return}
  S.habits.push({id:Date.now()+'',name:n,color:obColor,freq:obFreq,created:today()});
  save();startApp();
}

function hexRgb(h){const r=/^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(h);return r?`${parseInt(r[1],16)},${parseInt(r[2],16)},${parseInt(r[3],16)}`:null}
function applyAccent(c){S.accent=c;document.documentElement.style.setProperty('--accent',c);const rgb=hexRgb(c);if(rgb)document.documentElement.style.setProperty('--accent-rgb',rgb)}

function startApp(){
  applyAccent(S.accent);
  if(S.dark){document.documentElement.setAttribute('data-theme','dark');document.getElementById('dark-toggle').classList.add('on');document.getElementById('theme-btn').textContent='☀️'}
  document.getElementById('ob-screen').classList.remove('active');
  document.getElementById('app-screen').classList.add('active');
  initAccentPicker();renderAll();applyLang();
}

function initAccentPicker(){
  document.getElementById('accent-picker').innerHTML=ACCENTS.map(a=>`<div class="ac-opt${S.accent===a.c?' sel':''}" style="background:${a.c}" data-c="${a.c}" onclick="pickAccent(this)">✓</div>`).join('');
}
function pickAccent(el){
  document.querySelectorAll('.ac-opt').forEach(e=>e.classList.remove('sel'));el.classList.add('sel');
  applyAccent(el.dataset.c);save();renderAll();
}

function toggleTheme(){
  S.dark=!S.dark;
  document.documentElement.setAttribute('data-theme',S.dark?'dark':'');
  document.getElementById('dark-toggle').classList.toggle('on',S.dark);
  document.getElementById('theme-btn').textContent=S.dark?'☀️':'🌙';
  save();if(wChart){renderWeekChart()}
}

function toggleLang(){
  S.lang=S.lang==='de'?'en':'de';
  document.getElementById('lang-btn').textContent=S.lang.toUpperCase();
  document.getElementById('lang-toggle').classList.toggle('on',S.lang==='en');
  save();applyLang();renderAll();
}
function applyLang(){
  const l=S.lang;
  document.querySelectorAll(`[data-${l}]`).forEach(el=>el.textContent=el.getAttribute(`data-${l}`));
  document.getElementById('lang-btn').textContent=l.toUpperCase();
}
function t(de,en){return S.lang==='de'?de:en}
function freqLabel(i){return S.lang==='de'?FREQS_DE[i||0]:FREQS_EN[i||0]}

function navTo(p){
  document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));
  document.getElementById(`page-${p}`).classList.add('active');
  document.getElementById(`nav-${p}`).classList.add('active');
  if(p==='stats'){renderHeatmap();renderWeekChart();renderBadges()}
  if(p==='manage')renderManage();
}

function renderAll(){renderHeader();renderQuote();renderKPIs();renderWeek();renderTodayHabits();renderManage();renderBadges()}

function renderHeader(){
  const h=new Date().getHours();
  const g=S.lang==='de'?(h<12?'Guten Morgen':h<18?'Guten Tag':'Guten Abend'):(h<12?'Good morning':h<18?'Good afternoon':'Good evening');
  document.getElementById('h-greet').textContent=`${g}, ${S.name} 👋`;
}

function renderQuote(){
  const qs=S.lang==='de'?QUOTES_DE:QUOTES_EN;
  const q=qs[new Date().getDay()%qs.length];
  document.getElementById('quote-text').textContent=`"${q.t}"`;
  document.getElementById('quote-author').textContent=q.a;
}

function calcStreak(id){
  let s=0,d=new Date();
  if(!(S.log[dstr(d)]||[]).includes(id))d.setDate(d.getDate()-1);
  while(true){const k=dstr(d);if((S.log[k]||[]).includes(id)){s++;d.setDate(d.getDate()-1)}else break}
  return s;
}
function bestStreak(id){
  let best=0,cur=0;
  Object.keys(S.log).sort().forEach(k=>{if((S.log[k]||[]).includes(id)){cur++;best=Math.max(best,cur)}else cur=0});
  return best;
}
function overallBest(){return S.habits.reduce((m,h)=>Math.max(m,bestStreak(h.id)),0)}
function weekDoneCount(id){
  const ws=weekStart();let c=0;
  for(let i=0;i<7;i++){const k=dstr(addD(ws,i));if(k>today())break;if((S.log[k]||[]).includes(id))c++}
  return c;
}

function renderKPIs(){
  const tk=today(),done=(S.log[tk]||[]).length,total=S.habits.length;
  document.getElementById('kpi-done').textContent=done;
  document.getElementById('kpi-done-sub').textContent=`${t('von','of')} ${total} Habits`;
  document.getElementById('kpi-best').textContent=overallBest();
  document.getElementById('kpi-active').textContent=total;
  const ws=weekStart();let tp=0,td=0;
  for(let i=0;i<7;i++){const k=dstr(addD(ws,i));if(k>today())break;tp+=total;td+=(S.log[k]||[]).length}
  document.getElementById('kpi-rate').textContent=(tp>0?Math.round(td/tp*100):0)+'%';
}

function renderWeek(){
  const ws=weekStart(),tk=today(),total=S.habits.length||1;
  const DL_DE=['Mo','Di','Mi','Do','Fr','Sa','So'],DL_EN=['Mo','Tu','We','Th','Fr','Sa','Su'];
  const labels=S.lang==='de'?DL_DE:DL_EN;
  document.getElementById('week-days').innerHTML=Array.from({length:7},(_,i)=>{
    const d=addD(ws,i),k=dstr(d),done=(S.log[k]||[]).length,isFuture=k>tk,isToday=k===tk;
    const pct=done/total;
    let cls=isFuture?'future':pct>=1?'full':pct>0?'part':'none';
    if(isToday&&cls!=='full')cls+=' today';
    return `<div class="wday"><div class="wday-lbl">${labels[i]}</div><div class="wday-dot ${cls}">${done}</div></div>`;
  }).join('');
}

function renderTodayHabits(){
  const el=document.getElementById('today-habits');
  if(!S.habits.length){el.innerHTML=`<div class="empty"><div class="empty-icon">🌱</div><div class="empty-text">${t('Noch keine Habits – füge deine erste hinzu!','No habits yet – add your first one!')}</div></div>`;return}
  const dk=today(),dl=S.log[dk]||[];
  el.innerHTML=S.habits.map(h=>{
    const done=dl.includes(h.id),streak=calcStreak(h.id),wdone=weekDoneCount(h.id);
    return `<div class="habit-row${done?' done':''}">
      <div class="hcheck${done?' checked':''}" style="${done?`background:${h.color}`:`border-color:${h.color}`}" onclick="toggleHabit('${h.id}')">${done?'✓':''}</div>
      <div class="hinfo">
        <div class="hname">${h.name}</div>
        <div class="hmeta"><span class="hstreak">🔥 ${streak} ${t('Tage','days')}</span><span class="hfreq">${freqLabel(h.freq)}</span></div>
        <div class="hprog-wrap"><div class="hprog" style="width:${Math.min(wdone/7*100,100)}%;background:${h.color}"></div></div>
      </div>
      <div class="hdot" style="background:${h.color}"></div>
    </div>`;
  }).join('');
}

function toggleHabit(id){
  const k=today();if(!S.log[k])S.log[k]=[];
  const idx=S.log[k].indexOf(id),wasOff=idx===-1;
  if(wasOff)S.log[k].push(id);else S.log[k].splice(idx,1);
  save();renderTodayHabits();renderKPIs();renderWeek();
  if(wasOff&&[7,14,21,30,60,100].includes(calcStreak(id)))showConfetti();
}

function renderHeatmap(){
  const now=new Date(),yr=now.getFullYear(),mo=now.getMonth();
  const MO_DE=['Januar','Februar','März','April','Mai','Juni','Juli','August','September','Oktober','November','Dezember'];
  const MO_EN=['January','February','March','April','May','June','July','August','September','October','November','December'];
  document.getElementById('hm-month').textContent=S.lang==='de'?MO_DE[mo]:MO_EN[mo];
  const first=new Date(yr,mo,1),last=new Date(yr,mo+1,0),total=S.habits.length||1;
  let startDow=first.getDay();startDow=startDow===0?6:startDow-1;
  const cells=[];
  for(let i=0;i<startDow;i++)cells.push(null);
  for(let d=1;d<=last.getDate();d++){
    const k=`${yr}-${String(mo+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
    const done=(S.log[k]||[]).length,pct=done/total;
    cells.push(pct===0?0:pct<.25?1:pct<.5?2:pct<.75?3:4);
  }
  const cols=[];for(let i=0;i<cells.length;i+=7)cols.push(cells.slice(i,i+7));
  document.getElementById('hm-grid').innerHTML=cols.map(col=>`<div class="hm-col">${col.map(c=>c===null?'<div class="hm-cell" style="opacity:0"></div>':`<div class="hm-cell l${c}"></div>`).join('')}</div>`).join('');
}

function renderWeekChart(){
  const ws=weekStart(),total=S.habits.length||1;
  const DL_DE=['Mo','Di','Mi','Do','Fr','Sa','So'],DL_EN=['Mo','Tu','We','Th','Fr','Sa','Su'];
  const labels=S.lang==='de'?DL_DE:DL_EN;
  const data=Array.from({length:7},(_,i)=>{const k=dstr(addD(ws,i));return k>today()?0:Math.round((S.log[k]||[]).length/total*100)});
  const ctx=document.getElementById('chart-week').getContext('2d');
  if(wChart)wChart.destroy();
  wChart=new Chart(ctx,{type:'bar',data:{labels,datasets:[{data,backgroundColor:S.accent+'88',borderColor:S.accent,borderWidth:2,borderRadius:6}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{min:0,max:100,ticks:{callback:v=>v+'%',color:'#888',font:{size:10}},grid:{color:'rgba(128,128,128,.1)'}},x:{ticks:{color:'#888',font:{size:10}},grid:{display:false}}}}});
}

function renderBadges(){
  const best=overallBest();
  document.getElementById('badges-grid').innerHTML=BADGES.map(b=>`<div class="badge-item"><div class="badge-icon${best>=b.days?' earned':''}">${b.icon}</div><div class="badge-lbl">${S.lang==='de'?b.de:b.en}</div></div>`).join('');
}

function renderManage(){
  const el=document.getElementById('manage-list');
  if(!S.habits.length){el.innerHTML=`<div class="empty"><div class="empty-icon">📋</div><div class="empty-text">${t('Noch keine Habits vorhanden.','No habits yet.')}</div></div>`;return}
  el.innerHTML=S.habits.map(h=>`<div class="mh-row">
    <div class="mh-dot" style="background:${h.color}"></div>
    <div class="mh-info"><div class="mh-name">${h.name}</div><div class="mh-sub">🔥 ${calcStreak(h.id)} ${t('Tage','days')} · ${freqLabel(h.freq)}</div></div>
    <div class="mh-actions">
      <button class="mh-btn" onclick="openEditModal('${h.id}')">✏️</button>
      <button class="mh-btn del" onclick="deleteHabit('${h.id}')">🗑</button>
    </div>
  </div>`).join('');
  document.getElementById('add-btn').style.display=S.habits.length>=15?'none':'flex';
}

function openAddModal(){
  editId=null;mColor=ACCENTS[0].c;mFreq=0;
  document.getElementById('modal-title').textContent=t('Neue Gewohnheit','New Habit');
  document.getElementById('m-save').textContent=t('Hinzufügen','Add');
  document.getElementById('m-cancel').textContent=t('Abbrechen','Cancel');
  document.getElementById('modal-name').value='';
  initModalOpts();document.getElementById('habit-modal').classList.add('open');
  setTimeout(()=>document.getElementById('modal-name').focus(),300);
}
function openEditModal(id){
  editId=id;const h=S.habits.find(x=>x.id===id);mColor=h.color;mFreq=h.freq||0;
  document.getElementById('modal-title').textContent=t('Gewohnheit bearbeiten','Edit Habit');
  document.getElementById('m-save').textContent=t('Speichern','Save');
  document.getElementById('m-cancel').textContent=t('Abbrechen','Cancel');
  document.getElementById('modal-name').value=h.name;
  initModalOpts();document.getElementById('habit-modal').classList.add('open');
}
function initModalOpts(){
  document.getElementById('modal-colors').innerHTML=ACCENTS.map(a=>`<div class="ob-color${a.c===mColor?' sel':''}" style="background:${a.c}" data-c="${a.c}" onclick="pickObColor(this,'modal-colors');mColor=this.dataset.c">✓</div>`).join('');
  const freqs=S.lang==='de'?FREQS_DE:FREQS_EN;
  document.getElementById('modal-freqs').innerHTML=freqs.map((f,i)=>`<div class="ob-freq${i===mFreq?' sel':''}" data-i="${i}" onclick="pickObFreq(this,'modal-freqs');mFreq=parseInt(this.dataset.i)">${f}</div>`).join('');
  document.getElementById('m-cl').textContent=t('Farbe','Color');
  document.getElementById('m-fl').textContent=t('Häufigkeit','Frequency');
}
function saveHabit(){
  const n=document.getElementById('modal-name').value.trim();if(!n){document.getElementById('modal-name').focus();return}
  // Read selected color/freq from DOM
  const selC=document.querySelector('#modal-colors .sel');if(selC)mColor=selC.dataset.c;
  const selF=document.querySelector('#modal-freqs .sel');if(selF)mFreq=parseInt(selF.dataset.i);
  if(editId){const h=S.habits.find(x=>x.id===editId);h.name=n;h.color=mColor;h.freq=mFreq}
  else{if(S.habits.length>=15)return;S.habits.push({id:Date.now()+'',name:n,color:mColor,freq:mFreq,created:today()})}
  save();document.getElementById('habit-modal').classList.remove('open');renderAll();
}
function closeModalOv(e){if(e.target===document.getElementById('habit-modal'))document.getElementById('habit-modal').classList.remove('open')}
function deleteHabit(id){
  const h=S.habits.find(x=>x.id===id);
  if(!confirm(`"${h.name}" ${t('wirklich löschen?','really delete?')}`))return;
  S.habits=S.habits.filter(x=>x.id!==id);save();renderAll();
}

function showConfetti(){
  const wrap=document.getElementById('conf-wrap');wrap.innerHTML='';
  const colors=['#06D6A0','#FFD166','#EF476F','#7B61FF','#00B4D8'];
  for(let i=0;i<60;i++){
    const el=document.createElement('div');el.className='conf-piece';
    el.style.cssText=`left:${Math.random()*100}%;top:0;width:${6+Math.random()*6}px;height:${6+Math.random()*6}px;background:${colors[Math.floor(Math.random()*colors.length)]};border-radius:${Math.random()>.5?'50%':'2px'};animation-duration:${1.5+Math.random()*1.5}s;animation-delay:${Math.random()*.5}s;`;
    wrap.appendChild(el);
  }
  setTimeout(()=>wrap.innerHTML='',3500);
}

function resetAll(){
  document.getElementById('reset-modal').classList.add('open');
}
function confirmReset(){
  localStorage.removeItem('ht2_s');location.reload();
}
function cancelReset(){
  document.getElementById('reset-modal').classList.remove('open');
}

// INIT
load();initOb();
if(S.name){startApp()}
</script>

</body>
</html>
